# Pearson's Correlation Coefficient

cor(Dangling, DC,  method = "pearson", use = "complete.obs")
cor(Dangling, BC,  method = "pearson", use = "complete.obs")
cor(Dangling, CC,  method = "pearson", use = "complete.obs")
cor(Dangling, EVC,  method = "pearson", use = "complete.obs")
cor(Dangling, KC,  method = "pearson", use = "complete.obs")

# Spearman's Correlation Coefficient
cor(Dangling, DC,  method = "spearman", use = "complete.obs")
cor(Dangling, BC,  method = "spearman", use = "complete.obs")
cor(Dangling, CC,  method = "spearman", use = "complete.obs")
cor(Dangling, EVC,  method = "spearman", use = "complete.obs")
cor(Dangling, KC,  method = "spearman", use = "complete.obs")


# Kendall's Correlation Coefficient
cor(Dangling, DC,  method = "kendall", use = "complete.obs")
cor(Dangling, BC,  method = "kendall", use = "complete.obs")
cor(Dangling, CC,  method = "kendall", use = "complete.obs")
cor(Dangling, EVC,  method = "kendall", use = "complete.obs")
cor(Dangling, KC,  method = "kendall", use = "complete.obs")